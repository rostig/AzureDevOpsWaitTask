{
  "name": "wait-command",
  "version": "1.0.0",
  "description": "Azure DevOps task that waits until specified variable is created in currently running release or until defined timeout expired. Requires - Allow scripts to access the OAuth token. Works cross platform.",
  "main": "index.js",
  "author": "Rostislav Gerasimov",
  "license": "MIT",
  "dependencies": {
    "azure-pipelines-task-lib": "^2.9.3",
    "moment": "^2.29.4",
    "request": "^2.88.2",
    "request-promise-native": "^1.0.8"
  },
  "devDependencies": {}
}
